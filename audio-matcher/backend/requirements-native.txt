# Required for DawDreamerPluginHost (real VST3/AU automation) -- SimulatedPluginHost
# needs none of this. The 0.8.3 API (make_plugin_processor, set_parameter(index,
# normalized_value), load_graph, get_audio(name), save_state/load_state) was
# verified directly against this installed version; a real plugin binary was
# never available to render through it in the environment that wrote this, so
# only the API surface itself is confirmed, not an end-to-end render.
dawdreamer==0.8.3
